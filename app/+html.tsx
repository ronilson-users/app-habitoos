import { View, Text } from 'react-native';
react

interface Props {
  // Define your props here
}

const html = (props: Props) => {
  return (
    <View>
      <Text>FirstTab</Text>
    </View>
  );
};

export default html;